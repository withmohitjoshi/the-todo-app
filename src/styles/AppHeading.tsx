import styled from "styled-components";

const AppHeadingContainer = styled.div`
  text-align: center;
  cursor: pointer;
  h1 {
    color: var(--color-blue);
  }
`;
export default AppHeadingContainer;
